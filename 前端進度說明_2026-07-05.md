# 前端進度說明

更新日期：2026/07/05  
專案位置：`android_medical_frontend`

## 一、目前前端完成狀態

本階段前端已完成主要問診流程、醫師推薦流程、歷史紀錄顯示、醫師簡介資料整合，以及確認掛號資訊畫面優化。整體流程已可從首頁進入 AI 問診，完成問診後查看推薦醫師，選擇醫師後進入確認掛號頁。

目前主要使用 Kotlin + Jetpack Compose 開發，資料來源包含本地模擬資料、後端 API 回傳推薦資料，以及前端 assets 內的醫師簡介 JSON。

## 二、前端專案架構

前端專案位於：

```text
android_medical_frontend/
```

主要目錄如下：

```text
android_medical_frontend/
├── app/
│   └── src/
│       └── main/
│           ├── AndroidManifest.xml
│           ├── assets/
│           │   └── teacher_profiles.json
│           ├── java/com/example/medicalaiguidance/
│           │   ├── MainActivity.kt
│           │   ├── model/
│           │   ├── navigation/
│           │   ├── network/
│           │   ├── repository/
│           │   ├── screen/
│           │   ├── service/
│           │   ├── util/
│           │   └── viewmodel/
│           └── res/
├── build.gradle.kts
├── settings.gradle.kts
└── gradle/
```

### 1. `screen/`

負責 Jetpack Compose 畫面 UI。

目前主要畫面：

- `HomeScreen.kt`：首頁與近期紀錄。
- `ChatScreen.kt`：AI 問診、語音輸入、推薦按鈕。
- `DoctorSelectionScreen.kt`：醫師推薦、醫師卡片、醫師簡介 BottomSheet。
- `ConfirmNeedScreen.kt`：確認掛號資訊與前往掛號。
- `HistoryScreen.kt`：歷史紀錄列表與篩選。

### 2. `viewmodel/`

負責畫面狀態管理與呼叫 Repository。

主要 ViewModel：

- `HomeViewModel.kt`：取得近期紀錄。
- `ChatViewModel.kt`：管理問診對話、語音狀態、推薦按鈕狀態。
- `DoctorViewModel.kt`：取得推薦醫師、醫師簡介狀態。
- `ConfirmViewModel.kt`：取得確認掛號資訊。
- `HistoryViewModel.kt`：管理歷史紀錄列表與篩選。

### 3. `repository/`

集中處理前端資料來源與暫存狀態。

主要檔案：

- `MedicalRepository.kt`

目前負責：

- 呼叫後端 API。
- 管理目前問診 caseId。
- 暫存目前科別、醫師、時段。
- 儲存與讀取歷史紀錄。
- 讀取 `teacher_profiles.json` 醫師簡介資料。
- 組合 ConfirmNeedScreen 所需的 Appointment 資料。
- 推算下一次可看診日期。

### 4. `network/`

負責 API DTO 與 HTTP 請求。

主要檔案：

- `MedicalApiClient.kt`
- `MedicalDtos.kt`

目前支援：

- `/chat`
- `/recommend`
- `/generate_script`
- `/voice/chat`
- `/voice/tts`

### 5. `model/`

定義前端資料模型。

主要模型：

- `Appointment.kt`：確認掛號資訊。
- `ChatMessege.kt`：聊天訊息。
- `Department.kt`：科別資料。
- `Doctor.kt`：醫師資料。
- `DoctorProfile.kt`：醫師簡介資料。
- `History.kt`：歷史紀錄。

### 6. `navigation/`

定義 Compose Navigation 路由。

主要檔案：

- `NavGraph.kt`

目前路由：

```text
home
chat
chat/{historyId}
select_doctor
confirm_need
history
```

### 7. `service/`

與 Android 輔助工具服務相關。

主要檔案：

- `MyAccessibilityService.kt`
- `OverlayManager.kt`

用途：

- 前往台北榮總 App 後，提供紅框引導功能。
- 接收目標科別、醫師、時間等資訊。

### 8. `util/`

工具類別。

目前包含：

- `AudioRecorder.kt`
- `AudioPlayer.kt`

用途：

- 台語語音錄音。
- AI 回覆語音播放。

## 三、前端資料流架構

### 1. 問診流程資料流

```text
ChatScreen
→ ChatViewModel
→ MedicalRepository
→ MedicalApiClient
→ 後端 /chat
→ 回傳 triage / conversation_state / department_result
→ ChatViewModel 更新畫面狀態
```

當後端判斷問診已足夠推薦時：

```text
ChatViewModel
→ showDecisionButtons = true
→ ChatScreen 顯示「看推薦醫生 / 我想修改」
```

### 2. 推薦醫師資料流

```text
ChatScreen 點擊「看推薦醫生」
→ NavGraph 前往 select_doctor
→ DoctorSelectionScreen
→ DoctorViewModel
→ MedicalRepository.recommend(caseId)
→ 後端 /recommend
→ 回傳 specialty_first / time_first
→ DoctorSelectionScreen 顯示推薦卡片
```

目前 UI 顯示邏輯：

```text
specialty_first → 顯示為「科別優先」
time_first → 顯示為「時間優先」
```

### 3. 醫師簡介資料流

```text
DoctorSelectionScreen 點擊「醫師簡介」
→ DoctorViewModel
→ MedicalRepository.getDoctorProfileByName(name)
→ app/src/main/assets/teacher_profiles.json
→ DoctorSpecialtyContent 顯示簡介 BottomSheet
```

若找不到醫師簡介：

```text
DoctorSelectionScreen 隱藏「醫師簡介」按鈕
→ 只顯示「我要掛號」
```

### 4. 確認掛號資料流

```text
DoctorSelectionScreen 點擊「我要掛號」
→ MedicalRepository 暫存 currentDoctor / currentDepartment / currentTimeSlot
→ NavGraph 前往 confirm_need
→ ConfirmViewModel
→ MedicalRepository.getConfirmedAppointment()
→ ConfirmNeedScreen 顯示掛號資訊
```

預約日期推算：

```text
目前日期 + 醫師門診星期
→ 若本週尚未到該星期，使用本週日期
→ 若本週已過該星期，使用下週日期
```

## 四、畫面導航流程

目前主要導航流程：

```text
HomeScreen
  ↓
ChatScreen
  ↓
DoctorSelectionScreen
  ↓
ConfirmNeedScreen
```

歷史紀錄流程：

```text
HomeScreen
  ↓
HistoryScreen
  ↓
ChatScreen(historyId)
```

醫師推薦返回流程：

```text
DoctorSelectionScreen
  ↓ popBackStack()
ChatScreen 原本對話位置
```

此流程已保留原本「看推薦醫生 / 我想修改」按鈕，不會回到空白新對話。

## 五、首頁 HomeScreen

已完成項目：

- 首頁背景改為淡綠到白色的柔和漸層背景。
- 近期紀錄區塊可顯示最近問診紀錄。
- 已完成紀錄的主要標題會顯示科別名稱。
- 未完成紀錄維持顯示「症狀評估中」。
- 點擊近期紀錄可回到對應的歷史對話。

相關檔案：

- `app/src/main/java/com/example/medicalaiguidance/screen/HomeScreen.kt`
- `app/src/main/java/com/example/medicalaiguidance/viewmodel/HomeViewModel.kt`

## 六、問診 ChatScreen

已完成項目：

- 支援文字問診流程。
- 支援國語 / 台語語音模式切換。
- 國語 / 台語切換按鈕已加入綠色滑動動畫效果。
- AI 回覆完成並可推薦時，會顯示：
  - 「看推薦醫生」
  - 「我想修改」
- 從醫師推薦頁返回時，會回到原本對話位置，且保留「看推薦醫生 / 我想修改」按鈕。
- AI 回覆區保留警語提示。
- 支援播放 AI 回覆語音。

相關檔案：

- `app/src/main/java/com/example/medicalaiguidance/screen/ChatScreen.kt`
- `app/src/main/java/com/example/medicalaiguidance/viewmodel/ChatViewModel.kt`

## 七、醫師選擇 DoctorSelectionScreen

已完成項目：

- 醫師推薦頁可依對話結果顯示兩種排序模式：
  - 時間優先
  - 科別優先
- 原本「專長優先」文案已改為「科別優先」。
- 科別優先時，摘要文字顯示為「符合症狀的建議科別」。
- 推薦標籤文案已改為：
  - 「科別首選」
  - 「最快推薦」
- 推薦標籤底色使用提示色 `#FDF6EC`。
- 返回鍵樣式已與 HistoryScreen 保持一致。
- 返回鍵使用 `navController.popBackStack()`，可回到原本的問診對話。
- 醫師卡片底部在 Android 底部導覽列環境下已增加滑動留白，避免按鈕被遮擋。
- 預設模擬醫師資料中，名字下方已由職稱改為科別：
  - 蘇宇平：一般骨科
  - 邱方遙：一般骨科
  - 許逵翔：一般骨科

資料顯示邏輯：

- 如果後端推薦資料存在，優先顯示推薦醫師卡片。
- 如果後端推薦資料不可用，會 fallback 到本地預設醫師資料。
- 醫師卡片會優先顯示目前 AI 判斷出的科別。
- 若醫師沒有簡介資料，會隱藏「醫師簡介」按鈕，只顯示「我要掛號」。

相關檔案：

- `app/src/main/java/com/example/medicalaiguidance/screen/DoctorSelectionScreen.kt`
- `app/src/main/java/com/example/medicalaiguidance/viewmodel/DoctorViewModel.kt`
- `app/src/main/java/com/example/medicalaiguidance/repository/MedicalRepository.kt`

## 八、醫師簡介 BottomSheet

已完成項目：

- 已整合醫師簡介 JSON 資料。
- 點擊「醫師簡介」後顯示抽屜式 BottomSheet。
- 抽屜式介面包含：
  - 醫師姓名
  - 醫師照片
  - 職稱
  - 學歷
  - 專長
  - 經歷
- 醫師照片會從 `photo_url` 載入。
- 專長與經歷皆改為列點方式呈現。
- 專長與經歷內容已對齊。
- 經歷區塊可展開 / 收合。
- 「專長 / 經歷」膠囊標題陰影已調淡。
- BottomSheet 最大高度已限制，往上滑不會蓋住上方「選擇醫師」標題區域。
- BottomSheet 內容本身可垂直滑動，適用不同尺寸 Android 手機。

資料來源：

- `app/src/main/assets/teacher_profiles.json`

新增資料模型：

- `app/src/main/java/com/example/medicalaiguidance/model/DoctorProfile.kt`

## 九、歷史紀錄 HistoryScreen

已完成項目：

- 歷史紀錄頁支援全部 / 已完成 / 未完成切換。
- 返回鍵樣式作為其他頁面參考標準。
- 已完成紀錄仍顯示完成狀態。
- 未完成紀錄使用提示色。
- 點擊歷史紀錄可回到對應對話。

相關檔案：

- `app/src/main/java/com/example/medicalaiguidance/screen/HistoryScreen.kt`
- `app/src/main/java/com/example/medicalaiguidance/viewmodel/HistoryViewModel.kt`

## 十、確認掛號 ConfirmNeedScreen

已完成項目：

- 顯示確認掛號資訊：
  - 科別
  - 看診醫師
  - 預約時間
- 「看診醫師」下方已移除不必要的科別副標。
- 預約時間已改為根據使用者當下日期推算：
  - 如果門診星期本週還沒到，顯示本週日期。
  - 如果門診星期已經過了，顯示下週日期。
  - 如果剛好是今天，顯示今天。
- 前往掛號前會檢查輔助工具權限。
- 若權限未開啟，會提示使用者前往設定。
- 若權限已開啟，會顯示再次確認彈窗。

相關檔案：

- `app/src/main/java/com/example/medicalaiguidance/screen/ConfirmNeedScreen.kt`
- `app/src/main/java/com/example/medicalaiguidance/viewmodel/ConfirmViewModel.kt`
- `app/src/main/java/com/example/medicalaiguidance/repository/MedicalRepository.kt`

## 十一、資料整合狀態

### 1. 醫師簡介資料

已將抓取下來的醫師簡介資料放入前端 assets：

```text
app/src/main/assets/teacher_profiles.json
```

資料欄位包含：

- `name`
- `photo_url`
- `education`
- `current_positions`
- `experience`
- `specialty_tags`
- `titles`
- `tid`

目前前端會依醫師姓名查詢對應簡介資料。

### 2. JSON 清理

已清理學歷欄位中部分重複學位贅字，例如：

```text
國立陽明大學 醫學工程研究所博士 博士
```

已改為：

```text
國立陽明大學 醫學工程研究所博士
```

## 十二、目前已驗證項目

多次執行 Android build，確認目前前端可正常編譯：

```powershell
.\gradlew.bat :app:assembleDebug
```

最近一次驗證結果：

```text
BUILD SUCCESSFUL
```

## 十三、目前仍可優化項目

後續可再優化：

- 將本地模擬醫師資料全面改為後端推薦資料。
- 醫師簡介照片可改用 Coil 等圖片載入套件，提升快取與載入穩定性。
- 醫師簡介資料目前以姓名比對，未來可改用醫師 ID 或 tid 對應，避免同名問題。
- 科別與門診資料目前仍有部分 fallback 邏輯，未來可完全以後端資料為主。
- UI 可再針對不同 Android 螢幕尺寸做實機測試與微調。

## 十四、前端目前結論

目前前端已完成核心使用流程：

```text
首頁 → AI 問診 → 查看推薦醫師 → 醫師簡介 / 我要掛號 → 確認掛號 → 前往掛號
```

整體 UI 已具備可展示程度，並已針對返回流程、醫師簡介資料、預約時間推算、底部導覽列遮擋問題做過優化。
